// Summary of objects and eval
let summary = {
  what: "Objects group related data and actions.",
  why: "They help organize code.",
  eval: "eval() runs code from a string. Avoid using it unless necessary."
};
alert(summary.what + "\n" + summary.why + "\n" + summary.eval);

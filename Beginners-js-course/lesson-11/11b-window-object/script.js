// Show alert with current window size when button is clicked
document.getElementById('alertBtn').onclick = function() {
  alert('Window width: ' + window.innerWidth + '\nWindow height: ' + window.innerHeight);
};
